{
  "mcpServers": {
    "reflect-memory": {
      "command": "npx",
      "args": ["-y", "reflect-memory-mcp"],
      "env": {
        "REFLECT_MEMORY_API_KEY": "${REFLECT_MEMORY_API_KEY}"
      }
    }
  }
}
